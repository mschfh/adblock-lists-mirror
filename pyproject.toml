[project]
name = "adblock-lists-mirror"
version = "0.1.0"
description = "Tool to download and mirror adblock lists"
requires-python = ">=3.13"
dependencies = [
    "aiohttp==3.12.14",
    "requests==2.32.4",
    "sentry-sdk[aiohttp]==2.29.1",
]
[tool.uv]
required-version = "==0.9.4"
